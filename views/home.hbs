<div class="container">
  {{#each students}}
    <div class="student-list">
      <a href="/students/{{id}}" alt="{{name}}">
        <p>{{name}}</p>
        <div class='img-circle'> 
        <img src="{{image}}">
        </div>
      </a>
    </div>
  {{/each}}
</div>